You are working on the Memster codebase at /home/house/memster/. Make ALL of the following improvements to memster_mcp_server.py, .env.example, and README.md:

## 1. COMPLETE THE POSTGRESQL MIGRATION
The file memster_mcp_server.py currently uses sqlite3. Replace ALL sqlite3 usage with psycopg2:

- Replace 'import sqlite3' with 'import psycopg2' and 'import psycopg2.extras'
- Read DATABASE_URL from environment: os.environ.get("DATABASE_URL", "postgresql://memster:password@localhost:5432/memster") — set this as both DATABASE_PATH and DB_PATH
- get_db_connection() should return a psycopg2 connection using cursor_factory=psycopg2.extras.RealDictCursor
- Convert ALL '?' placeholders in SQL to '%s' (psycopg2 style)
- init_database(): replace INTEGER PRIMARY KEY AUTOINCREMENT with SERIAL PRIMARY KEY
- Replace the FTS5 virtual table and triggers with a PostgreSQL tsvector approach:
  * Add column: search_vector tsvector GENERATED ALWAYS AS (to_tsvector('english', content)) STORED
  * Add index: CREATE INDEX IF NOT EXISTS idx_memories_search ON memories USING GIN (search_vector)
  * Remove the 3 FTS triggers (memories_ai, memories_ad, memories_au) since tsvector is auto-generated
- Convert all 'INSERT OR REPLACE' to use PostgreSQL 'INSERT ... ON CONFLICT ... DO UPDATE' syntax
- In hybrid_search(): replace FTS5 query with: SELECT ... FROM memories WHERE search_vector @@ plainto_tsquery('english', %s)
- In handle_query(): replace the FTS5 JOIN with tsquery approach
- Update .env.example to show: DATABASE_URL=postgresql://memster:password@localhost:5432/memster

## 2. FIX DUPLICATE DETECTION PERFORMANCE
Replace the O(n^2) Python loop in find_near_duplicate_memories() with a PostgreSQL query using the pg_trgm extension:

- Add CREATE EXTENSION IF NOT EXISTS pg_trgm to init_database()
- Rewrite the function to use: SELECT a.id as id_a, b.id as id_b, similarity(a.content, b.content) as sim FROM memories a JOIN memories b ON a.id < b.id WHERE similarity(a.content, b.content) > %s ORDER BY sim DESC LIMIT 100
- Group results into clusters by transitive closure on shared IDs

## 3. ADD MEMORY CONFLICT DETECTION
Add a new function detect_memory_conflicts(new_content, network_type):
- Extract entity names (words 4+ chars, non-stopwords) from the content
- Check if content contains positive state indicators (is up, running, active, working, online, succeeded, passed, healthy, available, connected)
- Check if content contains negative state indicators (is down, broken, stopped, failing, offline, failed, error, unhealthy, unavailable, disconnected)
- If positive, search for existing memories in same network_type with same entities but negative patterns (and vice versa)
- Return up to 5 conflicts with existing_id, existing_content, conflict_entity, existing_t_event

Then modify handle_remember() to call this before insert and include 'conflicts_detected' in the response JSON. Do NOT block the insert if conflicts are found, only warn.

## 4. ADD AUTO IMPORTANCE SCORING
Add auto_score_importance(content, network_type) that returns float 0.0-1.0:
- Baselines: world=0.6, experience=0.5, opinion=0.4, observation=0.4
- IP addresses in content: +0.2
- File paths in content: +0.1
- Port numbers in content: +0.1
- URLs in content: +0.15
- Error/failure keywords (error, failed, crash, broken, fatal, timeout, exception, traceback, panic, corrupted): +0.15
- Content longer than 100 chars: +0.1
- Action + outcome pair detected (action words like fixed/resolved/solved/built/deployed + outcome words like working/running/success/completed): +0.15
- Cap at 1.0

Modify handle_remember() to call auto_score_importance() and include 'importance' in the INSERT.

## 5. ADD GRACEFUL EMBEDDINGS FALLBACK
- In handle_remember(): when EMBEDDINGS_AVAILABLE is False, add to response: "embeddings_unavailable": true, "fallback_mode": "keyword_search"
- Add a new tool definition for 'memster_embeddings_status' to TOOL_DEFINITIONS
- Add a handler that returns: embedding_model name, available status, and instructions for enabling NVIDIA NIM embeddings

## After making all changes:
1. Run: python3 -m py_compile /home/house/memster/memster_mcp_server.py
2. Run: python3 -c "import ast; ast.parse(open('/home/house/memster/memster_mcp_server.py').read()); print('AST OK')"
3. Update README.md to document PostgreSQL setup, conflict detection, importance scoring, and embeddings status tool

CRITICAL RULES:
- Preserve ALL existing functionality
- Keep all existing tool definitions and handlers intact
- Do NOT remove or rename any tools
- Ensure all SQL uses %s placeholders
- Ensure conn.cursor() creates RealDictCursor so row["column"] access works
- Make your best judgment on any edge cases — do not stop to ask questions